# 背景音乐

请将背景音乐文件放在当前目录，并命名为：

```text
background.mp3
```

完整路径：

```text
frontend/assets/music/background.mp3
```

游戏会尝试在页面加载时自动循环播放。如果浏览器禁止自动播放，用户第一次点击、触摸或按键后会自动开始播放。页面中不会显示音乐开关按钮。

建议：

- 使用 MP3 格式
- 建议文件大小控制在 `5MB` 以内
- 建议音乐时长在 `1–5` 分钟之间
- 建议导出为 `96–160kbps`，兼顾音质和移动端加载速度
