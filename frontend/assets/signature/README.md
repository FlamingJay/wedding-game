# 祝福签名横版照片

请将用于贴签名的横版照片放在当前目录，并命名为：

```text
signature-photo.jpg
```

完整路径：

```text
frontend/assets/signature/signature-photo.jpg
```

推荐规格：

- 横版比例优先使用 `16:9`、`3:2` 或 `4:3`
- 建议尺寸不低于 `1920 × 1080`
- 推荐格式：`.jpg` 或 `.webp`
- 请在照片中预留适合放置祝福文字或签名的空白区域
- 照片主体尽量不要贴近边缘，方便手机端裁切和定位
